---
phase: 01-design-system-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - FamilyLife/Views/Home/HomeView.swift
  - FamilyLife/Views/Home/GroceryListView.swift
  - FamilyLife/Views/Home/FoodKitchenView.swift
  - FamilyLife/Views/Calendar/CalendarView.swift
  - FamilyLife/Views/Calendar/WeekView.swift
  - FamilyLife/Views/Pantry/PantryView.swift
  - FamilyLife/Views/Expenses/ExpensesView.swift
  - FamilyLife/Views/Cook/CookView.swift
  - FamilyLife/Views/Trips/TripsView.swift
  - FamilyLife/Views/Rivalries/RivalriesView.swift
  - FamilyLife/Views/Rivalries/RivalryDetailView.swift
  - FamilyLife/Views/Decisions/DecisionsView.swift
  - FamilyLife/Views/Gifts/GiftsView.swift
  - FamilyLife/Views/Gifts/PersonGiftListView.swift
autonomous: true
requirements:
  - DS-05

must_haves:
  truths:
    - "No view file contains a direct .glassEffect() call on a rect-shaped card surface — all rectangular glass cards go through .flCard(tint:). Capsule-shaped chip surfaces (e.g. .glassEffect(..., in: .capsule)) are exempt and remain as-is."
    - "No view file contains a raw padding literal (e.g. .padding(12) or .padding(.horizontal, 16)) — all use DesignTokens.Spacing.*"
    - "No view file contains a raw .opacity() color fill — semantic colors use TabAccent or BadgeSemantic"
    - "The private StatPill in HomeView.swift is removed and replaced by import of the shared StatPill"
    - "The private FilterChip in DecisionsView.swift is removed and replaced by the shared FilterChip"
  artifacts:
    - path: "FamilyLife/Views/Home/HomeView.swift"
      provides: "Home dashboard using shared StatPill, SectionHeader, .flCard(tint: TabAccent.home.color)"
      contains: "StatPill\|SectionHeader\|flCard"
    - path: "FamilyLife/Views/Decisions/DecisionsView.swift"
      provides: "Decisions view using shared FilterChip from Components/"
      contains: "FilterChip"
  key_links:
    - from: "All view files"
      to: "FamilyLife/Views/Components/DesignTokens.swift"
      via: "DesignTokens.Spacing.* replacing raw padding values"
      pattern: "DesignTokens\\.Spacing\\."
    - from: "All view files with cards"
      to: "FamilyLife/Views/Components/FLCardModifier.swift"
      via: ".flCard(tint:) replacing inline .glassEffect() chains on rect surfaces"
      pattern: "\\.flCard\\("
---

<objective>
Apply the design system to all 14 view files: replace inline .glassEffect() on rectangular card surfaces with .flCard(tint:), replace raw padding literals with DesignTokens.Spacing constants, remove duplicate private component definitions, and update callers to use shared ButtonStyles.

Purpose: Enforces the invariant that only one file (FLCardModifier.swift) ever calls .glassEffect() on rect surfaces. Eliminates the ~25 copy-pasted glass card implementations and all raw magic numbers from view files.
Output: All view files use design system primitives. The private StatPill and FilterChip copies are removed from HomeView and DecisionsView.
</objective>

<execution_context>
@/Users/jessesharratt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jessesharratt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-foundation/01-02-SUMMARY.md
@.planning/phases/01-design-system-foundation/01-03-SUMMARY.md

@FamilyLife/Views/Components/DesignTokens.swift
@FamilyLife/Views/Components/FLCardModifier.swift
@FamilyLife/Views/Components/StatPill.swift
@FamilyLife/Views/Components/FilterChip.swift
@FamilyLife/Views/Components/ButtonStyles.swift
@FamilyLife/Views/Components/SectionHeader.swift
@FamilyLife/Views/Components/BadgeLabel.swift
</context>

<interfaces>
<!-- From Plan 02 — shared components now available: -->

From FamilyLife/Views/Components/FLCardModifier.swift:
```swift
func flCard(tint: Color = .clear, interactive: Bool = false) -> some View
```

From FamilyLife/Views/Components/DesignTokens.swift:
```swift
DesignTokens.Spacing.sectionGap       // 24pt
DesignTokens.Spacing.cardGap          // 12pt
DesignTokens.Spacing.horizontalMargin // 20pt
DesignTokens.Spacing.cardPadding      // 14pt

DesignTokens.CornerRadius.card        // 16pt
DesignTokens.Opacity.cardTint         // 0.1

TabAccent.home.color     // .teal
TabAccent.calendar.color // .purple
TabAccent.pantry.color   // .cyan
TabAccent.expenses.color // .orange
TabAccent.trips.color    // .blue
TabAccent.cook.color     // .orange
TabAccent.rivalries.color // .red
TabAccent.decisions.color // .indigo
TabAccent.gifts.color    // .pink
```

From FamilyLife/Views/Components/StatPill.swift:
```swift
struct StatPill: View { init(title: String, value: String, icon: String, color: Color) }
```

From FamilyLife/Views/Components/FilterChip.swift:
```swift
struct FilterChip: View { init(_ label: String, isSelected: Bool, tint: Color = .teal, action: () -> Void) }
```

From FamilyLife/Views/Components/ButtonStyles.swift:
```swift
Button("Save") {}.buttonStyle(.flPrimary(tint: .teal))
Button("Cancel") {}.buttonStyle(.flSecondary)
Button("Delete") {}.buttonStyle(.flDestructive)
```

From FamilyLife/Views/Components/SectionHeader.swift:
```swift
struct SectionHeader: View { init(title: String, subtitle: String? = nil, icon: String? = nil) }
```

From FamilyLife/Views/Components/BadgeLabel.swift:
```swift
struct BadgeLabel: View { init(_ text: String, semantic: BadgeSemantic) }
enum BadgeSemantic { case overdue, expiringSoon, done, info, custom(Color) }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate HomeView, GroceryListView, FoodKitchenView, CalendarView, WeekView</name>
  <files>
    FamilyLife/Views/Home/HomeView.swift
    FamilyLife/Views/Home/GroceryListView.swift
    FamilyLife/Views/Home/FoodKitchenView.swift
    FamilyLife/Views/Calendar/CalendarView.swift
    FamilyLife/Views/Calendar/WeekView.swift
  </files>
  <action>
For each file, read it fully, then make these targeted changes:

**HomeView.swift (highest priority — has private StatPill to remove):**
1. Find the private `struct StatPill` definition (around line 369) and DELETE it entirely — the shared one in Components/ replaces it
2. Find every `GlassEffectContainer` usage — these are a custom wrapper. Per research, keep GlassEffectContainer as-is for now (it wraps multiple items under one shared glass surface, which is different from individual item cards). Do NOT attempt to remove GlassEffectContainer in this phase.
3. Find every `.glassEffect(.regular.tint(color.opacity(X)), in: .rect(cornerRadius: 16))` call that wraps an INDIVIDUAL item card and replace with `.flCard(tint: color)`
4. Find every `.glassEffect(.regular.tint(color.opacity(X)).interactive(), in: .rect(cornerRadius: 16))` and replace with `.flCard(tint: color, interactive: true)`
5. Replace raw `.padding(X)` with `DesignTokens.Spacing.cardPadding` where X is the card padding value
6. Replace `.padding(.horizontal, X)` with `.padding(.horizontal, DesignTokens.Spacing.horizontalMargin)` where X = 16 or 20
7. Replace raw `.opacity(0.1)` color tints that were part of glass card calls (now handled by .flCard internally)

Replacement mapping for this file:
- `.glassEffect(.regular.tint(.orange.opacity(0.1)), in: .rect(cornerRadius: 16))` → `.flCard(tint: TabAccent.expenses.color)`
- `.glassEffect(.regular.tint(.blue.opacity(0.1)), in: .rect(cornerRadius: 16))` → `.flCard(tint: TabAccent.trips.color)`
- `.glassEffect(.regular.tint(.blue.opacity(0.2)), in: .capsule)` → KEEP AS-IS (capsule shape chip — exempt from rect-card migration rule)
- `.glassEffect(.regular.tint(.purple.opacity(0.1)), in: .rect(cornerRadius: 16))` → `.flCard(tint: TabAccent.calendar.color)`
- `.glassEffect(.regular.tint(.green.opacity(0.1)), in: .rect(cornerRadius: 16))` → `.flCard(tint: TabAccent.home.color)` (or .green if it's a status indicator)

After changes, verify HomeView still compiles and StatPill usages now reference the shared component (not the deleted private one).

**GroceryListView.swift and FoodKitchenView.swift:**
Read each file. Apply the same three changes:
1. Replace `.glassEffect(...)` on individual item cards (rect surface only) with `.flCard(tint: tabColor)` using the appropriate tab tint
2. Replace raw padding literals with DesignTokens.Spacing constants
3. Replace `.borderedProminent` or `.bordered` button styles with `.flPrimary(tint:)` / `.flSecondary` on any prominent CTA buttons

**CalendarView.swift and WeekView.swift:**
Same three changes as above. CalendarView uses purple as its tab color (`TabAccent.calendar.color`). Look for appointment card glass effects specifically.

**For ALL files:** Do NOT change the overall view structure or navigation. Only swap glass effect calls, padding constants, and button styles. The visual output should look nearly identical — just using the design system now.
  </action>
  <verify>
`grep -n "glassEffect" FamilyLife/Views/Home/HomeView.swift FamilyLife/Views/Home/GroceryListView.swift FamilyLife/Views/Home/FoodKitchenView.swift FamilyLife/Views/Calendar/CalendarView.swift FamilyLife/Views/Calendar/WeekView.swift 2>/dev/null | grep -v "GlassEffectContainer\|AmbientBackground\|\.capsule"` should return 0 results.
`grep -n "struct StatPill" FamilyLife/Views/Home/HomeView.swift` should return 0 results (private copy removed).
All 5 files compile without errors.
  </verify>
  <done>HomeView private StatPill removed. All 5 files use .flCard(tint:) for rect-shaped item cards, DesignTokens.Spacing for padding. No inline glassEffect calls remain on rect card surfaces in these files. Capsule-shaped chips preserved.</done>
</task>

<task type="auto">
  <name>Task 2a: Migrate straightforward view files — PantryView, ExpensesView, CookView, TripsView, GiftsView, PersonGiftListView</name>
  <files>
    FamilyLife/Views/Pantry/PantryView.swift
    FamilyLife/Views/Expenses/ExpensesView.swift
    FamilyLife/Views/Cook/CookView.swift
    FamilyLife/Views/Trips/TripsView.swift
    FamilyLife/Views/Gifts/GiftsView.swift
    FamilyLife/Views/Gifts/PersonGiftListView.swift
  </files>
  <action>
Apply the standard three-change migration to these 6 view files. These files have no special cases (no private component definitions to remove, no capsule-shaped chips).

**Tab color assignments:**
- PantryView → `TabAccent.pantry.color` (.cyan)
- ExpensesView → `TabAccent.expenses.color` (.orange)
- CookView → `TabAccent.cook.color` (.orange)
- TripsView → `TabAccent.trips.color` (.blue)
- GiftsView + PersonGiftListView → `TabAccent.gifts.color` (.pink)

**PantryView.swift special case (layout):**
PantryView currently uses `List` with `.listStyle(.insetGrouped)`. Per research pitfall 3, do NOT convert this to LazyVStack — List is already lazy. Only replace any `.glassEffect()` calls that exist outside the List rows, and replace raw padding constants.

**For each of the 6 files:**
1. Read the file fully
2. Replace `.glassEffect(.regular.tint(color.opacity(X)), in: .rect(cornerRadius: Y))` on individual item cards with `.flCard(tint: tabColor)`
3. Replace `.glassEffect(.regular.tint(color.opacity(X)).interactive(), ...)` on rect surfaces with `.flCard(tint: tabColor, interactive: true)`
4. Replace raw `.padding(X)` and `.padding(.horizontal/vertical, X)` with DesignTokens.Spacing constants where X matches a token value (cardPadding=14, cardGap=12, sectionGap=24, horizontalMargin=20)
5. Replace `.borderedProminent` button style on CTA buttons with `.buttonStyle(.flPrimary(tint: tabColor))`
6. Replace `.bordered` on secondary buttons with `.buttonStyle(.flSecondary)`
7. Do NOT replace `.plain` button style — those are often for icon buttons or list row buttons that should stay plain
  </action>
  <verify>
`grep -rn "glassEffect" FamilyLife/Views/Pantry/ FamilyLife/Views/Expenses/ FamilyLife/Views/Cook/ FamilyLife/Views/Trips/ FamilyLife/Views/Gifts/ --include="*.swift" | grep -v "AmbientBackground\|\.capsule" | wc -l` returns 0.
All 6 files compile without errors.
  </verify>
  <done>6 straightforward view files migrated. No inline glassEffect calls remain on rect card surfaces. All padding uses DesignTokens.Spacing constants. CTA buttons use FLPrimaryButtonStyle or FLSecondaryButtonStyle.</done>
</task>

<task type="auto">
  <name>Task 2b: Migrate special-case files — RivalriesView, RivalryDetailView, DecisionsView (has private FilterChip)</name>
  <files>
    FamilyLife/Views/Rivalries/RivalriesView.swift
    FamilyLife/Views/Rivalries/RivalryDetailView.swift
    FamilyLife/Views/Decisions/DecisionsView.swift
  </files>
  <action>
Apply the same three-change migration to these 3 files. These files have special cases that need extra care.

**Tab color assignments:**
- RivalriesView + RivalryDetailView → `TabAccent.rivalries.color` (.red)
- DecisionsView → `TabAccent.decisions.color` (.indigo)

**RivalriesView.swift:**
Read it. Apply the standard migration. Note: if this file uses a ForEach inside ScrollView, it should already have LazyVStack from Plan 03 (Task 2). Do NOT add a second LazyVStack. Only apply glass card and padding changes.

**RivalryDetailView.swift:**
Read it. Apply standard migration. No private component definitions expected.

**DecisionsView.swift (has private FilterChip to remove — this is the key special case):**
1. Read the file fully
2. Find the private `struct FilterChip` definition and DELETE it — the shared one in Components/ replaces it
3. Verify the callsites still compile against the shared FilterChip signature: `FilterChip(_ label: String, isSelected: Bool, tint: Color = .teal, action: () -> Void)`
4. If the existing private FilterChip has a different parameter signature, update the callsites to match the shared signature
5. Apply the standard glass card and padding migration for the rest of the file

**For all 3 files — standard migration steps:**
1. Replace `.glassEffect(.regular.tint(color.opacity(X)), in: .rect(cornerRadius: Y))` on item cards with `.flCard(tint: tabColor)`
2. Replace `.glassEffect(.regular.tint(color.opacity(X)).interactive(), ...)` on rect surfaces with `.flCard(tint: tabColor, interactive: true)`
3. Replace raw `.padding(X)` and `.padding(.horizontal/vertical, X)` with DesignTokens.Spacing constants
4. Replace `.borderedProminent` / `.bordered` button styles with `.flPrimary` / `.flSecondary`
5. Do NOT replace `.plain` button style

**After all 3 files updated:**
Run a final audit across all view files: `grep -rn "glassEffect" FamilyLife/Views/ --include="*.swift" | grep -v "Components/\|AmbientBackground\|\.capsule"` — this should return 0 results. If any remain, fix them before declaring done.
  </action>
  <verify>
`grep -rn "glassEffect" FamilyLife/Views/ --include="*.swift" | grep -v "FamilyLife/Views/Components/\|AmbientBackground\|\.capsule" | wc -l` returns 0.
`grep -n "struct FilterChip" FamilyLife/Views/Decisions/DecisionsView.swift` returns 0 (private copy removed).
All 3 files compile without errors.
  </verify>
  <done>All 3 special-case files migrated. Private FilterChip removed from DecisionsView. No inline glassEffect calls remain on rect card surfaces in any view file. All button CTAs use FLPrimaryButtonStyle or FLSecondaryButtonStyle.</done>
</task>

</tasks>

<verification>
Final phase verification — all DS-05 criteria met:

1. `grep -rn "glassEffect" FamilyLife/Views/ --include="*.swift" | grep -v "Components/\|AmbientBackground\|\.capsule"` → 0 results
2. `grep -rn "struct StatPill" FamilyLife/Views/ --include="*.swift" | grep -v "Components/"` → 0 results
3. `grep -rn "struct FilterChip" FamilyLife/Views/ --include="*.swift" | grep -v "Components/"` → 0 results
4. `grep -rn "DateFormatter()" FamilyLife/ --include="*.swift"` → 0 results (from Plan 03)
5. `ls FamilyLife/Views/Components/` shows: AmbientBackground.swift, DesignTokens.swift, FLCardModifier.swift, SectionHeader.swift, BadgeLabel.swift, StatPill.swift, FilterChip.swift, ButtonStyles.swift, DateFormatters.swift

All app views build without errors.
</verification>

<success_criteria>
- Zero inline .glassEffect() calls remain on rect card surfaces in any view file — all routed through .flCard(tint:)
- Capsule-shaped chip surfaces (e.g. in HomeView) preserved as-is — they are not rect cards
- Zero raw magic number padding literals in view files — all use DesignTokens.Spacing.*
- Private StatPill removed from HomeView.swift, shared StatPill imported
- Private FilterChip removed from DecisionsView.swift, shared FilterChip imported
- CTA buttons in all views use FLPrimaryButtonStyle, secondary actions use FLSecondaryButtonStyle
- All 8 requirements (DS-01 through DS-05, PERF-01 through PERF-03) are complete after this plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-04-SUMMARY.md`
</output>
