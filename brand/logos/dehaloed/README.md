Kit logo PNGs with the translucent light-grey glow the raw exports carry removed (partially transparent, low-saturation, light pixels cleared; ink edges keep their evergreen hue so antialiasing is untouched), then trimmed. The wordmark and horizontal lockup also carry a faint opaque greenish drop shadow behind the letters that is left as-is — removing it damages the letter edges. Nothing in the artwork was redrawn. iOS `Brand/` imagesets are built from these.
